## 1. Scroll-scrub structure

- [x] 1.1 Wrap the chapter-1 column in `page.tsx` in a track element (~280vh runway) and make the hero+belt column `sticky top-0 h-svh` <!-- HeroTrack client component (sections/hero/track.tsx) so page.tsx stays a server component; sticky uses min-height:100svh (not fixed h-svh) to keep the pre-change short-viewport growth. Verified: belt pins for the full 1458px runway (stickyTop 0), releases cleanly, chapter 2 enters normally -->
- [x] 1.2 Under `prefers-reduced-motion: reduce`, collapse the track to a single viewport height (no dead scroll runway); verify the poster-only hero is unchanged <!-- also collapses under --mobile (mobile keeps the looping clip, so a dead runway would be the same trap). Verified via emulation: no <video>, trackHeight == viewport, poster renders inside the shell -->
- [x] 1.3 Verify the chapter theme morph (plum → cream) still fires at the belt → why-that-works boundary with the taller chapter 1 <!-- verified: plum #892f53 at track end (y=1458), cream once why-that-works crosses the center band -->
- [x] 1.4 Verify short-viewport behavior (~600px tall): hero keeps its 620px floor, belt below the fold, no layout breakage while pinned <!-- verified at 660px and 600px heights: hero section 662px, belt below fold, no breakage -->

## 2. Scrubbed video

- [x] 2.1 Replace the hero's `<Video>` usage with a hero-local `<video>` (muted, `playsInline`, `preload="auto"`, no loop, never played) plus the existing poster/reduced-motion image branch; leave `@/components/ui/video` untouched <!-- desktop only; mobile still renders the shared <Video> primitive (bare looping clip, aspectRatio 0.92, pre-change presentation) per 3.6 -->
- [x] 2.2 Wire hamo `useScrollTrigger` on the track: `onProgress` stores `target = min(1, progress / 0.8)` (HOLD = 0.2) <!-- trigger lives in HeroTrack; target flows to the hero via a context-provided mutable ref (no per-scroll re-renders) -->
- [x] 2.3 Add the rAF seek loop: lerp `currentTime` toward `target × (duration − 0.05)`, skip writes when delta < 0.02s; start at lerp factor 0.35 and tune against Lenis by feel <!-- useTempus (house frame loop) instead of raw rAF; shipped at 0.35 — verified converging without snap, final by-feel pass on real hardware still welcome -->
- [x] 2.4 Verify scrub behavior in the browser: forward and backward scrubbing is smooth, fast scroll jumps converge without snapping, no seeks occur at rest, hold phase parks on the final frame <!-- verified numerically: progress→time mapping exact, instant 1310px jump converges, currentTime frozen at rest (t1==t2 over 800ms), hold parks at ~4.97/5.04s -->
- [x] 2.5 Confirm the headline stagger still runs once on mount, decoupled from video time <!-- mount-based GSAP tween unchanged; nothing references video time -->
- [x] 2.6 Settle and record the final runway length, HOLD, and lerp constants (they parameterize the clip prompt in group 4) <!-- RECORDED: runway 280vh (hero.module.css .track), HOLD 0.2 (track.tsx), SCRUB_LERP 0.35 + SEEK_THRESHOLD 0.02s (sections/hero/index.tsx) -->

## 3. Retro TV shell (raster prototype in the working tree is the starting point)

- [x] 3.1 Promote the raster shell into shippable form: optimize the chosen Seedream cutout (`public/assets/tv-shell-prototype.png`, ~4MB PNG) into an alpha-capable web format (WebP/AVIF) sized for its rendered width, rename from `-prototype`, and render it through the project's image component <!-- public/assets/tv-shell.webp, 1360×1279 (2× the 680px rendered cap), cwebp q82, 98KB (was 2.2MB); rendered via @/components/ui/image with preload; source PNG preserved as assets/tv-shell-source.png in this change dir -->
- [x] 3.2 Fine-tune the measured screen rect (left/top/width/height percentages + glass border-radius) so the video sits under the bezel rim with no corner peek and no edge gap; verify at 1280–1680px widths <!-- measured rect (12% / 43% / 57.8% / 44.6%, radius 9%/13%) held as-is — verified clean at 1280/1440/1680: no corner peek, no edge gap -->
- [x] 3.3 Keep the glass overlays (glare, scanlines, edge vignette) above the video, clipped to the screen radius, `pointer-events: none` and `aria-hidden` <!-- .tvGlass span inside the aria-hidden .tvScreen; border-radius: inherit clips to the glass curve -->
- [x] 3.4 Wrap all media states in the shell: scrubbed video, pre-load poster, reduced-motion poster <!-- shell wraps: desktop scrubbed <video> (poster attr covers pre-load), and a poster <Image> for SSR/pre-mount/reduced-motion; verified reduced-motion shows poster in shell -->
- [x] 3.5 Resolve the headline-over-TV overlap (cap the headline clamp or make the layering deliberate); check at 1280–1680px widths <!-- DECISION: deliberate layering — headline z-indexed above the glass; the llama turns to look at the words, so text-over-screen reads as intent. Verified 1280/1440/1680: text never covers the llama itself -->
- [x] 3.6 Decide and implement mobile presentation (likely: shell desktop-only, mobile keeps the bare looping clip until the mobile follow-up) <!-- as predicted: shell + scrub desktop-only; mobile renders the shared <Video> (looping hero-mobile.mp4, 0.92 box, 65% crop bias — pre-change presentation) and the track collapses on --mobile -->
- [x] 3.7 Record the screen's final aspect and rendered crop, and keep the measurement script + generation brief with the change (they parameterize the clip framing in group 4 and any future shell re-roll) <!-- RECORDED: screen box = 57.8% × 44.6% of the 1360×1279 asset ≈ 786×570px → aspect 1.379; clip 1440×1080 (1.333) cover-fits with ~3.4% vertical crop. measure-screen-rect.py + tv-shell-generation.md + tv-shell-source.png live in this change's assets/ -->

## 4. Impressed clip generation
<!-- SEQUENCING DEVIATION (2026-07-12, user-directed): generated ahead of groups 1–3.
     The original gate dissolved on inspection: the scrub maps clip time proportionally
     (fixed 80/20 split), so runway/lerp constants don't affect where the impressed beat
     parks, and the screen aspect was already settled by the raster shell (1.37 ≈ 4:3). -->

- [x] 4.1 Crop frame 0 of the current `hero.mp4` to a head-and-chest 4:3 composition as the image-to-video source frame (upscale if below generation-input resolution) <!-- DEVIATION: used the identity reference (uploaded hero-poster.jpg, media 2d3287dd-a234-46a3-a4f3-020ba0f493de) + prompted "medium close-up, head and chest filling the frame" instead of an upscaled crop — avoids feeding a low-res crop as start frame; framing came out correct -->
- [x] 4.2 Get a model recommendation (`models_explore action:'recommend'`) and check credit cost per take against the ~590-credit balance <!-- seedance_2_0 (identity default), 45 credits/take at 5s/1080p/4:3/std; 389 credits at generation time -->
- [x] 4.3 Generate a 5s take: slow screen-left head turn for ~75% of the clip with the eyeline clearly exiting the frame toward the headline, then impressed beat (ears straight, mouth drops open, slight pull-back, eyebrows above the sunglass frames; glasses stay on; stable non-distracting background — exact color match not required) <!-- job 0f18d4c8-0bf6-4c7e-8bbd-782c0a8fc6ed; a "IN THE DARK" preset recommendation was declined (declined_preset_id) -->
- [x] 4.4 Review the take against the acceptance checklist (emotion readable in final 25%, eyeline correct, framing fills the 4:3 screen, no wardrobe drift); iterate the prompt, budget 2–4 takes max <!-- take 1 ACCEPTED: frame 0 head-and-chest facing camera; turn completes ~75%; impressed profile (mouth open, eyebrow above frames, ears up) holds to end; eyeline exits screen-left; glasses + shirt consistent -->
- [x] 4.5 Post-process the winning take with ffmpeg: trim → crop/scale to the TV screen's 4:3 (e.g. 1440×1080) → all-intra h264 (`-g 1`) at 24fps, ≤ ~5MB → extract poster jpg from frame 0 <!-- no trim needed (full 5s arc); 1664×1248 → 1440×1080, crf 24, 3.7MB; poster from frame 0 -->
- [x] 4.6 Verify encoding (ffprobe: all frames pict_type I); no background gate — the TV vignette absorbs drift <!-- 121/121 I-frames -->
- [x] 4.7 Swap `public/clips/hero.mp4` + `hero-poster.jpg`, confirm the llama fills the TV screen pre-scroll, and scrub the full runway in the browser to confirm the impressed pose lands in the hold phase <!-- swap DONE and fills-the-screen verified live (autoplay loop interim); hold-phase verified after groups 1–2: parked at 90% runway progress, the llama holds the impressed profile (mouth open, ears up, eyebrow above frames) facing the headline -->

