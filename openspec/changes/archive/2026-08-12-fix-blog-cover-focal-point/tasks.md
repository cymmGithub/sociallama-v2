# fix-blog-cover-focal-point — tasks

## 1. Focal point pass-through

- [x] 1.1 Add a `focalPosition(media)` helper next to `resolveMedia` in `lib/payload/queries.ts`: returns `{ objectPosition: "X% Y%" }` when the row carries explicit focal values, `undefined` otherwise (null/undefined → no style, browser default 50/50). *Payload stamps `focalX`/`focalY` = 50 on every upload, so "no focal point" is 50/50 rather than null — the helper folds that case back to `undefined` too.*
- [x] 1.2 Post header: pass the helper's result as `style` on the cover `Image` in `app/(frontend)/[slug]/post-article.tsx`.
- [x] 1.3 Hub surfaces: same pass-through in `hub-featured.tsx`, `post-card.tsx`, and `hub-popular.tsx` (check `hub-video.tsx` for a cover crop while there). *`hub-video.tsx`'s poster is an `object-fit: cover` crop of a media row too, so it got the same pass-through; `hub-popular.tsx` has two call sites (lead card + short-list rows).*
- [x] 1.4 Verify no-regression: a cover with default/null focal renders byte-identical styles to today (no `object-position` emitted). *Helper probed across null/undefined/50-50/off-centre inputs (only off-centre returns a style) and all 528 dev-DB media rows are 50/50, so no surface emits `object-position` before the editorial pass. Typecheck and Biome clean.*

## 2. Header ratio (design D3)

- [x] 2.1 `app/(frontend)/[slug]/post.module.css` — `.cover` `aspect-ratio: 4 / 3` → `16 / 10`, with the comment noting why the header now matches the master ratio.
- [x] 2.2 Verify the header shape on a long post and a short one, both locales, desktop and mobile: the cover still bleeds past the stage's bottom edge and the meta column beside it does not reflow. *At 1440px the cover box goes 445px → 371px; `.headerGrid`'s `align-items: end` keeps its bottom on the stage edge with the square corners intact, and the copy column is untouched (its own height and `.meta`'s bottom margin set the stage). Mobile stacks as before. Biome clean.*

## 3. Editorial audit and repair

- [x] 3.1 Sweep the published posts' headers (Playwright screenshot loop over `/blog` slugs from the local dev DB, both locales for one spot-check) and list covers whose subject is cut by the 4:3 crop. *Retargeted: the local dev DB holds only 15 synthetic fixtures (all 2560×1600 masters composed to spec), so the sweep ran against the **prod** DB's 79 published posts / 73 distinct covers, read-only. Each master was composited with the live crop windows (4:3 header, 16:10 card, 16:9 lead) using the same clamped-window maths as `object-fit: cover`. At 4:3 the verdicts were 3 fixable by focal point, 3 needing a recomposed master, 3 borderline, 64 clean — which is what surfaced D3: the 4:3 box crops 62 of 73 covers horizontally, against 14 at 16:10. Re-measured at 16:10 the "recomposed master" and "borderline" groups both empty out, leaving 3 focal-point adjustments. Sheet: https://claude.ai/code/artifact/b66ffa53-a08e-4be2-bec3-ddd0b6c8b0da*
- [x] 3.2 Set focal points in the Payload admin for the three offenders — values retuned for the 16:10 header, since the 4:3 values would be wrong: `2020-03-br-1.png` → 50/12, `2020-02-InkedIce-Dream-rtm_LI-1.jpg` → 70/50, `2017-05-Brutto-Grosika-Jodła-Show-i-bezglutenowe-ciastka.jpeg` → 20/50. Re-screenshot to confirm the header, and check the hub card + featured lead for any adjusted cover. *Written to the prod DB one row at a time via a throwaway `payload.update` script, media 135 / 147 / 297; re-read confirms exactly those three off centre and the other 70 untouched. Header and card now share the 16:10 window, so one check covers both.*
- [x] 3.3 Record every adjustment (media id, filename, old→new focal) in the image audit artifact, per the blog-cover-art bookkeeping requirement. *`content/media/image-audit.json` — a `focalPoint` block on each of the three existing entries (from/to/setAt/change/reason plus the og state). Entry keys untouched, diff confined to those three objects.*
- [x] 3.4 Spot-check one adjusted post's OG image (Payload regenerates the cropped `og` size on focal change). ***The premise was wrong and the spot-check is what caught it.*** *Media 297's `sizes.og` came back byte-identical after the write (77 411 B, same filename): Payload only re-runs the resizer for an upload carrying `uploadEdits`, and a Local API `update` has no file. The live surfaces re-crop (they read `object-position` at render time); the share image keeps its centered crop. The other two rows have no og size at all — their masters are smaller than every cropped target. Recorded in the proposal, in design's risk list, and per-row in the audit artifact.*

## 4. Verify

- [x] 4.1 `bun run check` and the blog-related e2e specs from the worktree. *`check` exits 0 (648 tests, tsc clean, manifest current; the Biome output still carries the known pre-existing panics on 5 files, which do not affect the exit code). `pl-blog` + `en-blog` e2e: 8 passed, 3 skipped — the skips are the EN post cases, and the dev DB has no published EN posts.*
- [x] 4.2 Repeat the 3.1 sweep after the ratio change and the repairs: no published post header cuts its cover's subject. *All 73 covers re-rendered at 16:10 with the focal points now stored in prod and reviewed on contact sheets. The three adjusted covers read complete; the six the ratio change alone rescued (full-width captions and the logo sequence) are clean without any stored point; the remaining 64 are unchanged from the first sweep.*
