## 1. Measure

- [x] 1.1 Write `lib/styles/scripts/audit-tokens.ts`: walk `app/`, `components/`, `lib/` for `*.module.css`; print per-property distinct values with counts (font-size, padding/gap/margin, border-radius, box-shadow, transition/animation durations, hex colours); add a `styles:audit` script to `package.json`. Verify: output reproduces the 2026-09-11 baseline (442 font-size declarations, 178 distinct; 531 distinct spacing; ~25 shadows; 8 durations).
  - Baseline did not reproduce: the figures above came from explore-phase greps. Script output on 2026-09-11 (62 modules): font-size 463 / 191 distinct; spacing 1155 / 387; border-radius 215 / 54; box-shadow 38 / 29; duration 172 / 41 (literals only — `var(--duration-*)` uses are not times); hex 64 / 32. `DESIGN.md` quotes these.
- [x] 1.2 Locate the single consumer of the existing type-scale classes (`h1`/`h2`/`p`/`p-big`/`caption`/`cta`/`link`) and record its computed font-size, weight and line-height at 375px and 1440px.
  - Not a single consumer. `h2` is live on four `/o-nas` headings ("O LAMIE", "Ostatnio zrealizowane projekty", "GOOD ONE", the team surname) via `about-intro`, `projects`, `good-one`, `team`; the paired module classes set no font property, so the utility owns the type. Computed (Exo 2): 375px → 32px / 700 / 25.6px / -0.96px; 1440px → 48px / 700 / 38.4px / -1.44px. `link` is referenced only by the unmounted Shopify cart modal. `h1`, `p`, `p-big`, `caption`, `cta` have no consumer.
- [x] 1.3 Check whether any existing class uses a Tailwind default `rounded-*` or `shadow-*` utility; decide between resetting the namespace and prefixing tokens `--radius-sl-*` (design D2/risks).
  - No default renders: `css/index.css` imports `tailwindcss/utilities.css` only, never `theme.css`, so `rounded` (skip link, `ui/error-view`) and `rounded-md` (`ui/select`) emit no rule today. Resetting the namespace is safe and role names stay unprefixed — but never `sm`/`md`/`lg`/`xl`/…, which would switch those dormant classes on. Also found: Tailwind drops unused `@theme` variables (`--ease-in-circ` is absent from the compiled CSS), so `tokens.css` must be `@theme static` or its tokens resolve to nothing.
- [x] 1.4 Capture before-screenshots of `/`, one case study, one blog post at 375px and 1440px (headless Chromium, `ignoreCache`), stored in the scratchpad.
  - Four pages, not three: `/`, `/case-studies/irobot`, `/social-selling-index-linkedin` and `/o-nas`, which task 1.2 showed is the only page rendering a type-scale utility. A fresh Playwright context per width needs no `ignoreCache`. Each capture also dumps 22 computed properties for every element in `body` plus the type-role consumers' metrics. Taken in the reverse order the task assumes: the first session's shots were lost with the wiped scratchpad, so the *after* state was captured first, then the work was parked in a WIP commit, the tracked files restored to the proposal commit, `setup:styles` re-run, and the *before* state captured against the same dev server.

## 2. Extract

- [x] 2.1 Cluster font sizes by role and pick canonicals per design D4; write the roles (`display`, `title`, `lead`, `body`, `small`, `caption`, `label` at minimum) into `lib/styles/typography.ts`, removing the Satus defaults. Run `bun setup:styles`.
  - Eight roles written, each with the family/weight/line-height/tracking observed travelling with its canonical size (`type-lead` was unanimous: 6/6 rules identical). Two deviations from the design: **(a)** utilities are prefixed `type-*`, because Tailwind emits a custom utility for any candidate string it finds in any scanned file, so a bare `.body`/`.title`/`.small` would style every element that happens to carry that class name; **(b)** `h2` is kept byte-identical instead of removed — it has four rendered `/o-nas` consumers (task 1.2), and removing it would restyle a shipped page. `h1`/`p`/`p-big`/`caption`/`cta`/`link` had no rendered consumer and are gone (`link`'s only reference is the unmounted Shopify cart modal). `TypeStyles['font-size']` now also accepts a string, which is how the site's own `clamp()`/rem sizes are expressed; the generator already passed strings through.
- [x] 2.2 Cluster radii, shadows and durations; write `lib/styles/css/tokens.css` as a hand-authored `@theme` block (`--radius-*`, `--shadow-*`, `--duration-*`) and import it from `css/index.css`.
  - 11 tokens: radius chip 12 / card 18 / panel 20 / pill 999px / circle 50%; the ink-tinted shadow ramp card / raised / overlay; durations fast 200ms / 400ms / slow 800ms. Two deviations: **(a)** `@theme static`, because Tailwind drops theme variables it sees no use of (`--ease-in-circ` is absent from the compiled CSS) and CSS modules compile separately, so a plain `@theme` would leave `var(--radius-card)` resolving to nothing; **(b)** the three `--duration-*` definitions were **moved** out of `css/global.css` at their shipped values rather than redefined, which would have left two conflicting sets of the same names. No namespace reset: `index.css` imports only `tailwindcss/utilities.css`, so there is no default radius/shadow scale to reset (task 1.3). Verified with a standalone `@tailwindcss/cli` compile — all 11 land in the output.
- [x] 2.3 Read the context of the near-miss hexes (`#d9812a`, `#a83c65`, `#8d2f53`) and the other non-brand literals; classify each as social-brand colour, deliberate shade, or drift. Record, change nothing.
  - 32 distinct literals, none of them drift away from the palette. **Platform/partner brand:** `#0a66c2` LinkedIn, `#1877f2` Facebook, `#e60023` Pinterest, `#f9ce34`/`#ee2a7b`/`#6228d7` the Instagram gradient, `#000` TikTok (`ui/social-links`), `#ed4956` the Instagram heart and `#ff2e5b`/`#21e6ff` the hero's mock UI, and the `/uslugi` partner accents `#f4c430`, `#f2695b` (folks), `#41ad49`. **Deliberate shades:** `#d9812a` is the orange CTA's hover deepen (kontakt + zostan-lama, commented); `#a83c65`/`#8d2f53`/`#601c37` are the three stops of join-cta's radial plum ramp; `#241318` and `#2b1a08` are the page-local `--band-ink` grounds; `#8f838b` and `#bfacb6` are `--muted` (the footer's lightened for 4.5:1, commented); `#ff6b5b` is `--field-error`, chosen warm over the kit's cool default; `#0b0b0e` is the service page's dark ground; `#1c1c1c`/`#4a4a4a`/`#737373` are join-cta's phone-mock chrome. **Satus kit leftovers** (only in components the site barely renders): `#dc2626` in form fields, alert-dialog, `#22c55e`/`#3b82f6` in toast. `#fff`/`#000` are mostly mask layers (`linear-gradient(#000 0 0)`), not colour.
- [x] 2.4 Derive the spacing rhythm from the inventory (which steps dominate, section paddings by chapter, gap conventions). Document only; no spacing tokens unless a clear scale emerges.
  - The rhythm is already tokenized: spacing is overwhelmingly `var(--gap)` / `var(--safe)` (16px, viewport-scaled) times a factor — `gap*2` 29, `gap/2` 23, `safe*3` 22, `gap*1.5` 21, `safe*1.5` 16, `safe*2` 14, `safe*4` 11, `safe*3.5` 11, `gap*3` 11, `gap*0.75` 10. Section rhythm: `padding-inline: var(--safe)` with `padding-block: calc(var(--safe) * N)`, N = 3–6 on the home chapters (services 3/4, testimonial 6, news-lama 2/6, marquee 4) and 4 across `/o-nas`. So no new spacing tokens: the convention is the factor, and the odd steps (`*1.2`, `*1.4`, `*3.5`) are the drift worth naming in DESIGN.md. Raw rem spacing (0.5rem 18, 0.4rem 15, 1rem 14) stays inside components.

## 3. Document

- [x] 3.1 Write `DESIGN.md` (under ~300 lines): palette with role + origin table; chapter themes; type roles; spacing rhythm; radius/elevation tiers with tokens; motion (durations, easings, reduced-motion rule); layout grid (4/12 columns, 16px gap and safe, 800px breakpoint, header heights); iconography (Lucide only) and imagery rules (`.shot` 18px, baked mockup radii, pointer to `CLAUDE.md`); component vocabulary (button, pill, chip, card, band); standing rules with enforcement pointers; "existing variants, do not reuse" list; inventory date.
- [x] 3.2 Add `## Design Context` (≤ ~15 lines) to project `CLAUDE.md`: palette, faces, chapter themes, the two hard rules, "read `DESIGN.md` before styling".
- [x] 3.3 Add a `DESIGN.md` row to `AGENTS.md` Documentation Map and a pointer in `lib/styles/README.md` (tokens.css alongside easings.css; `styles:audit`).
  - `DESIGN.md` is 250 lines. `CLAUDE.md`'s `## Design Context` is 17 lines and sits above `## Project facts`; `lib/styles/README.md` gains the tokens.css family (with the `@theme static` reason) and the `styles:audit` pointer.
- [x] 3.4 Apply the `brand-theme` spec delta (the change's `specs/brand-theme/spec.md` is folded in at archive).
  - Nothing to write: the delta is authored and `openspec archive` folds it into `openspec/specs/brand-theme/spec.md`. Re-checked against the code — `plum-hero` `#913155`, `ink-deep` `#161216`, Exo 2 300/400/700/800 and Manrope 400/600/700 all match `colors.ts` / `fonts.ts`.

## 4. Verify

- [x] 4.1 `bun run check` passes (Biome, TypeScript, styles setup); `bun run build` passes; revert the Blob-stripping `importMap.js` side effect before staging.
  - `bun run check` exit 0 (Biome clean, tsc clean, 810 pass / 1 skip / 0 fail, COMPONENTS.md up to date); `bun run build` exit 0 with `/`, `/en`, `/o-nas`, `/en/about-us` still pure static. This build left `app/(payload)/admin/importMap.js` untouched — nothing to revert.
- [x] 4.2 Compare after-screenshots against 1.4 for the three pages at both widths: pixel-identical. Confirm the 1.2 consumer's computed values are unchanged.
  - **Pixel-identical is not achievable and never was.** Two captures of the *same* unchanged code differ by 3k–148k pixels (home and `/o-nas` at 1440), from the hero video, canvas work and late image paint; the per-element computed-style dump of the same two runs differed by 0 lines. So the gate is: computed styles identical for every element, plus a pixel diff no worse than that noise floor.
  - **Result: passed on all eight captures.** Computed styles 0 differing lines and consumer metrics 0 differing lines everywhere; pixels 0 on the case study and the blog post at both widths, and 17398 / 73945 (home) and 9715 / 35829 (`/o-nas`) against a measured noise floor of 21302 / 147790 and 9481 / 148274. `/o-nas`'s four `h2` headings are byte-identical in the consumer dump, and `transition-duration` across 1282 `/o-nas` elements has the identical distribution before and after, so moving the duration definitions out of `global.css` changed nothing.
  - Positive control, so that a zero diff cannot be an HMR no-op: the served CSS now carries all five `--radius-*`, all three `--shadow-*`, all three `--duration-*` (Lightning CSS minifies them to `.2s`/`.4s`/`.8s`) and the `.type-label` utility — none of which existed in the reverted tree.
- [x] 4.3 `bun run styles:audit` output matches the counts quoted in `DESIGN.md`.
  - Exact match on all six families (463/191, 1155/387, 215/54, 38/29, 172/41, 64/32) over 62 modules.
