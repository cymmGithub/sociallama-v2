## 1. Clip generation (Higgsfield — runs first; the bare layout can't ship against the current clip's background)

- [x] 1.1 Get a model recommendation (`models_explore action:'recommend'`) and confirm credit cost per take against the remaining balance (~344 expected; 45/take previously on seedance_2_0) <!-- seedance_2_0 confirmed via recommend; balance was 272.41 at start, 45/take at 5s/1080p/4:3/std -->
- [x] 1.2 Generate a 5s take from the llama identity reference with the wide composition: head-and-neck right-of-center, torso entering from the bottom edge, generous FLAT uniform plum field on the left; motion prompt: only the neck and head rotate slowly screen-left across ~70% of the clip (shoulders and torso perfectly still), eyeline exits frame-left; final ~30%: ears perk to attention, mouth parts slightly — explicitly no mouth-drop, no head pull-back, no eyebrow theatrics; glasses stay on <!-- job 69914070-5ad6-4529-a208-84d4cd30f6ea; "IN THE DARK" preset declined again (declined_preset_id); identity ref 2d3287dd-a234-46a3-a4f3-020ba0f493de; output 1664x1248, 121 frames -->
- [x] 1.3 Review the take at rendered hero size against the acceptance checklist (motion isolation, subtle beat readable on the final frame, eyeline, left plum field present, background flat — corner samples agree within gate tolerance, wardrobe continuity); iterate the prompt, budget 2–4 takes, pause for user decision after 4 rejections <!-- take 1 (job 69914070) passed the checklist but was REJECTED BY USER on feel ("tragic"). USER DECISION: reuse the 2026-07-12 take (job 0f18d4c8) trimmed instead — checklist acceptance is necessary, user feel is the final arbiter. 45 credits spent, balance ~227 -->

## 2. Post-pipeline (winning take → committed assets)

- [x] 2.1 Trim to 4s: choose the second to drop at review so the trimmed clip's FINAL frame is the subtle-impressed pose (the hold parks on it) <!-- FINAL: hard tail-trim of the 0f18d4c8 take at t=4.0s (96 frames) — cuts the maximal-hold second of the theatrical beat; final frame = open-mouth impressed profile. 3.5s was tried and judged over-trimmed by user. (Take-1 era retime approach recorded in git history) -->
- [x] 2.2 Crop/scale to the wide reference geometry (~1.27:1, e.g. 1370×1080) if the generation ratio differs <!-- FINAL: crop=1583:1248:0:0 on the 0f18d4c8 take (drops the source RIGHT margin, pushing the llama right-of-frame) then scale 1370x1080 -->
- [x] 2.3 Measure the take's flat background (corner sampling) and apply the global ffmpeg color grade mapping it to `#892f53`; reject the take and return to 1.2 if the required shift exceeds ~10 per RGB channel <!-- measured (129,44,80) vs token (137,47,83) → base grade +8/+3/+3, then +2/+2/+3 empirical bump for the browser-rendered match (final lutrgb +10/+5/+6); encode tagged smpte170m (Chrome decodes untagged/HD ambiguously; 709 conversion+tags rendered WORSE and was reverted) -->
- [x] 2.4 Encode all-intra h264 (`-g 1`) at 24fps, ≤ ~5MB; extract the poster jpg from frame 0 of the tinted encode <!-- crf 24, 2.77MB @4.0s, poster = straight frame-0 extraction (an image-space compensated poster overshot and was reverted) -->
- [x] 2.5 Gate: `bun lib/scripts/verify-clip-bg.ts public/clips/hero.mp4 '#892f53'` passes on the final encode; ffprobe reports every frame pict_type I <!-- gate PASSED (dE<=1.42) + browser-rendered verification: page (137,47,83) vs video (137,47,83) at 1440; clip bg has +-4-5 spatial variance (generation lighting falloff) so a faint edge is perceivable at some widths — accepted, same class as the reference build -->
- [x] 2.6 Swap `public/clips/hero.mp4` + `public/clips/hero-poster.jpg`

## 3. Presentation revert (bare right-anchored clip, reference geometry)

- [x] 3.1 Remove the TV shell from `app/(home)/sections/hero/index.tsx`: drop the `role="img"` shell wrapper, screen div, and glass span; render the desktop scrubbed `<video>` (and the SSR/pre-mount/reduced-motion poster `Image`) bare in a right-anchored absolute box (reference geometry: anchored bottom-right of the hero, full hero height below the header band, width auto); keep the scrub wiring (`useTempus` loop, context ref) byte-identical <!-- done; USER ADJUSTMENT: top inset safe*3.5+53px (smaller), right -safe*2 (bleeds the clip's empty right margin off-screen; only plum bleeds at 1280+) -->
- [x] 3.2 Update `hero.module.css`: delete `.tvShell`, `.tvShellImg`, `.tvScreen`, `.tvMedia`, `.tvGlass`; add the right-anchored video/poster styles; restore/adapt the `.copy` overlap comment (headline overflow lands on the clip's empty plum half — invisible seam again); keep `.mobileVideo` and the track/sticky styles untouched <!-- done; .media is mobile-only now -->
- [x] 3.3 Delete `public/assets/tv-shell.webp`; remove the hero exemption note from `lib/scripts/verify-clip-bg.ts` and restore the hero example in its usage docstring <!-- done -->
- [x] 3.4 Verify mobile (bare looping clip, unchanged) and reduced-motion (bare poster, collapsed track) still render correctly <!-- verified: mobile hero-mobile.mp4 looping bare, track collapsed; reduced: no <video>, poster, track = 1 viewport -->

## 4. Verification (against the final clip)

- [x] 4.1 Desktop browser pass at 1280/1440/1680: no visible clip edge against `#892f53` (motion and reduced), headline overflow lands on empty plum, llama never sits under the header band, belt pins, chapter 2 enters normally <!-- verified 1280/1440/1680; caveat recorded: faint residual edge possible from the clip's own bg variance; user to eyeball on real display -->
- [x] 4.2 Scrub pass: forward/backward smooth, fast jumps converge, no seeks at rest, hold parks on the subtle-impressed final frame <!-- verified: mapping exact (scrollY700 -> t2.089 vs want 2.07), settled at rest, hold parks t3.94/4.0 on the impressed profile -->
- [x] 4.3 Short viewport (~600px) sanity check while pinned <!-- verified at 1440x600 -->
- [x] 4.4 `bun run check` (Biome + tsc + tests + manifest) passes <!-- 371 tests pass, biome/tsc clean, manifest current (ran pre-final-CSS-tweak; biome+tsc re-run after) -->
